# Contributing to Mirror Loop

## Development setup
1. Clone the repo and install: `pip install -e .[dev]`
2. Run tests: `pytest`

## Coding standards
- Keep diffs small and focused.
- Add tests for new behavior; existing tests must pass.
- Match the surrounding style.

## Submitting changes
Open a PR against `main` with a clear description of the change and motivation.
