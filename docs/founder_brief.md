---
type: stream_doc
title: founder_brief
stream: mirror-loop
updated: '2026-05-26T09:21:02Z'
summary: 'Consensus: all agents converge on the same M1 spine: `game/` is canonical,
  `python -m game` must run end-to-end, and the slice is questionnaire → Act 1 → Recali'
---

Consensus: all agents converge on the same M1 spine: `game/` is canonical, `python -m game` must run end-to-end, and the slice is questionnaire → Act 1 → Recalibration → re-aimed Act 2 → escape ending.

Tension: Engineering frames PR1 as runtime proof after PR0; Infra splits hygiene into PR0/PR0a/PR0b before behavioral tests; CoS emphasizes that repo state was not verified in this run. Reconciliation: do PR0 anti-phantom controls first, then immediately verify disk/runtime reality before content expansion.

Out of scope: no LLM loop integration, no A/B playtest harness, no old `mirror/` architecture work, no broad determinism backlog unless retargeted to the current `game/` path.
